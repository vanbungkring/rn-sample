import {
  Dimensions,
} from 'react-native';

export default {
  window: {
    width: Dimensions.get('window').width,
    height: Dimensions.get('window').height,
  },
  navBarHeight: 70,
  tabBarHeight: 56,
  btnBtmDonateHeight: 50,
};
